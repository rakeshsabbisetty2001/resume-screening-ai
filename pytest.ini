[pytest]
# Without this, CI's bare `pytest -q` fails with ModuleNotFoundError on a
# clean checkout — `python -m pytest` (used locally) adds cwd to sys.path
# automatically, but plain `pytest` doesn't, and tests/ has no __init__.py
# to make pytest's rootdir-insertion fall back to the repo root on its own.
pythonpath = .
